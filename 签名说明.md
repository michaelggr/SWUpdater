# Android 签名密钥说明

## ⚠️ 重要提醒
请妥善保管此文件和签名密钥！丢失后无法更新应用！

---

## 签名文件信息

| 项目 | 值 |
|------|-----|
| **Keystore 文件** | `release.jks` |
| **Key Alias（别名）** | `swupdater` |
| **Keystore 密码** | `swupdater123` |
| **Key 密码** | `swupdater123` |
| **有效期** | 10,000 天（约 27 年） |
| **算法** | RSA 2048 位 |

---

## 文件位置

```
g:\dev\SWUPDATER\SWUpdater\
├── release.jks              ← 签名密钥文件
├── keystore_base64.txt      ← Base64 编码（用于 GitHub Secrets）
└── 签名说明.md               ← 本文档
```

---

## 如何在 GitHub Actions 中使用

### 步骤 1：打开仓库设置
1. 打开仓库 → **Settings**
2. 左侧菜单 → **Secrets and variables** → **Actions**
3. 点击 **New repository secret**

### 步骤 2：添加以下 4 个 Secrets

| Secret Name | Secret Value |
|-------------|--------------|
| `KEYSTORE_BASE64` | 打开 `keystore_base64.txt`，复制全部内容 |
| `KEYSTORE_PASSWORD` | `swupdater123` |
| `KEY_ALIAS` | `swupdater` |
| `KEY_PASSWORD` | `swupdater123` |

### 步骤 3：重新发布版本
推送一个新的 tag 即可触发签名构建！

---

## 本地测试签名

在项目根目录运行：

```bash
./gradlew assembleRelease
```

---

## 备份建议

✅ 建议备份到以下位置：
- 云端网盘（加密压缩）
- U 盘
- 另一台电脑

⚠️ **不要**提交到 Git 仓库！
