# 魔灵召唤 · 自动更新 — 开发文档

> **版本**: v1.7.0
> **包名**: `com.swupdater`
> **最低 SDK**: 24 (Android 7.0)
> **目标 SDK**: 34 (Android 14)
> **语言**: Kotlin
> **构建工具**: Gradle 8.x + Kotlin 1.9.x

---

## 一、项目概述

魔灵召唤 · 自动更新是一款面向中国地区魔灵召唤（Summoners War）玩家的游戏更新管理工具。核心功能包括：

- **版本检测** — 从配置的数据源URL自动检测最新游戏版本
- **APK 下载** — OkHttp 引擎，支持断点续传、进度显示、速度计算
- **通知栏进度** — 下载/校验/安装全程通知栏实时更新
- **WiFi 自动下载** — 发现新版本时，WiFi 环境下自动下载更新包
- **完整性校验** — 下载后自动校验文件大小、MD5、SHA256
- **Root 自动安装** — 已 Root 设备下载完成后自动静默安装，无需手动确认
- **系统安装** — 非 Root 设备自动处理安装权限，FileProvider 安全安装
- **壁纸系统** — 随机壁纸、壁纸下载、透明度控制
- **后台保活** — 前台服务 + WakeLock + 开机自启 + Root 保活
- **自动检查** — WorkManager 定时检查版本更新

---

## 二、项目结构

```
SWUpdater/
├── app/
│   ├── build.gradle.kts           # 应用模块构建配置
│   └── src/main/
│       ├── AndroidManifest.xml    # 清单文件
│       └── java/com/swupdater/
│           ├── SWUpdaterApp.kt    # Application 入口
│           ├── model/
│           │   └── Models.kt      # 数据模型（VersionInfo, DownloadState 等）
│           ├── network/
│           │   ├── VersionCheckService.kt  # 版本检测服务（v6）
│           │   ├── DownloadManager.kt      # 下载管理器（全局单例，OkHttp）
│           │   └── DownloadChannels.kt     # 下载渠道配置
│           ├── receiver/
│           │   ├── BootReceiver.kt    # 开机自启广播
│           │   └── InstallReceiver.kt # 安装完成广播
│           ├── service/
│           │   ├── DownloadService.kt          # 下载前台服务
│           │   ├── DownloadNotificationHelper.kt # 通知栏进度助手（全局单例）
│           │   ├── KeepAliveService.kt         # 后台保活服务
│           │   └── VersionCheckWorker.kt       # WorkManager 定时检查 + 自动下载
│           ├── ui/
│           │   ├── MainActivity.kt       # 主界面
│           │   ├── MainViewModel.kt      # 主界面 ViewModel
│           │   └── SettingsActivity.kt   # 设置界面
│           └── util/
│               ├── AppInfoUtil.kt      # 应用信息检测
│               ├── AppLog.kt           # 日志工具
│               ├── ChecksumUtil.kt     # 文件校验工具
│               ├── CommonUtil.kt       # 通用工具
│               ├── FileUtil.kt         # 文件与格式化工具
│               ├── RootInstallHelper.kt # Root 静默安装工具
│               └── WallpaperManager.kt # 壁纸管理器
├── res/
│   ├── layout/
│   │   ├── activity_main.xml      # 主界面布局
│   │   └── activity_settings.xml  # 设置界面布局
│   ├── drawable/                  # 矢量图标
│   ├── values/
│   │   ├── colors.xml             # 日间模式颜色
│   │   ├── dimens.xml             # 尺寸定义
│   │   ├── strings.xml            # 字符串资源
│   │   └── themes.xml             # 主题样式
│   ├── values-night/
│   │   ├── colors.xml             # 夜间模式颜色
│   │   └── themes.xml             # 夜间主题
│   ├── menu/main_menu.xml         # 工具栏菜单
│   ├── xml/file_paths.xml         # FileProvider 路径配置
│   └── mipmap-*/                  # 应用图标
└── build.gradle.kts               # 根构建配置
```

---

## 三、架构设计

### 3.1 整体架构

```
┌─────────────┐     ┌──────────────┐     ┌──────────────────┐
│  UI Layer    │────▶│  ViewModel   │────▶│  Network/Service │
│  (Activity)  │◀────│  (StateFlow) │◀────│  Layer           │
└─────────────┘     └──────────────┘     └──────────────────┘
       │                    │                      │
       ▼                    ▼                      ▼
  ViewBinding         Lifecycle            OkHttp + Jsoup
  Snackbar            Coroutine            WorkManager
  WallpaperManager    SharedPreferences    ForegroundService
```

### 3.2 数据流

```
用户点击"检查更新"
    │
    ▼
MainViewModel.checkUpdate()
    │
    ▼
VersionCheckService.checkLatestVersion(context)
    │  1. 从 SharedPreferences 读取数据源 URL
    │  2. 访问短链 → 解析 HTML → 提取 APK 直链
    │  3. 从 APK 文件名提取版本号 (smon_919 → 9.1.9)
    │  4. HEAD 请求获取文件大小
    │  5. dn.qpyou.cn 强制 HTTP 协议
    │
    ▼
VersionInfo(versionName, downloadUrl, fileSize)
    │
    ▼
MainViewModel.compareVersions() → UpdateCheckResult
    │
    ▼
MainActivity 观察 StateFlow 更新 UI
```

### 3.3 下载流程

```
用户点击"立即下载" / WiFi下自动下载
    │
    ▼
MainViewModel.startDownload()
    │
    ▼
DownloadService.start(context, url, versionName)
    │  1. 清除旧缓存
    │  2. 创建前台通知
    │  3. DownloadManager.startDownload()（全局单例）
    │     ├── URL 协议修正 (dn.qpyou.cn → HTTP)
    │     ├── OkHttp + IPv4 优先 DNS
    │     ├── 断点续传 (Range header)
    │     ├── 实时进度 (StateFlow → 通知栏 + UI)
    │     └── 0KB 校验 + 空文件清理
    │
    ▼
下载完成 → DOWNLOADED
    │
    ▼
MainViewModel.verifyDownloadedFile()
    │  ├── 文件大小校验
    │  ├── MD5 校验
    │  └── SHA256 校验
    │
    ▼
校验通过 → VERIFIED
    │
    ├── [Root自动安装开启] → RootInstallHelper.installSilently()
    │       │                     su -c "pm install -r -g"
    │       ├── 成功 → 通知"安装完成" → stopSelf()
    │       └── 失败 → 通知"安装失败" → stopSelf()
    │
    └── [非Root/未开启] → 通知"下载完成" → stopSelf()
            │
            ▼
       用户点击安装 → FileProvider → 系统安装器
```

---

## 四、核心模块详解

### 4.1 VersionCheckService (v6)

**职责**：从设置中配置的数据源URL检测最新版本

| 方法 | 说明 |
|------|------|
| `checkLatestVersion(context)` | 标准模式：返回 `VersionInfo?` |
| `checkAllSourcesWithDetails(context)` | 日志模式：逐步检测并返回每步结果 |

**版本号提取逻辑**：

```
APK URL: http://dn.qpyou.cn/smon/smon_919_NucRxWzPu7G4C2.apk
                                        ^^^
文件名提取: smon_919 → 919 → 9.1.9
规则: 3位 → X.Y.Z, 4位+ → X.Y.ZZ...
```

**APK 链接解析**：短链页面中的 JS `location.href` 或 HTML 中的 `.apk` URL

**IPv4 优先 DNS**：`Ipv4PreferredDns` 内部类，解决国内 IPv6 连接超时问题

**URL 协议处理**：

```kotlin
// dn.qpyou.cn 不支持 HTTPS，强制 HTTP
private fun ensureHttps(url: String): String {
    if (url.contains("dn.qpyou.cn")) {
        return url.replace("https://", "http://")
    }
    return url
}
```

### 4.2 DownloadManager（全局单例）

**职责**：APK 文件下载，支持断点续传

> **关键设计**：使用 `object` 声明全局单例，ViewModel 和 DownloadService 共享同一实例，
> 确保 UI 进度条和通知栏始终显示相同的下载进度。

| 特性 | 实现 |
|------|------|
| 单例模式 | `object DownloadManager`，全局唯一 |
| 下载引擎 | OkHttp + `Ipv4PreferredDns` |
| 断点续传 | `Range: bytes={已下载}-` 请求头 |
| 进度反馈 | `StateFlow<DownloadProgress>` (0-100%) |
| 速度计算 | 1秒采样间隔，`bytes/s` |
| 取消下载 | `Job.cancel()` + `isCancelled` 标志 |
| 0KB 校验 | 下载后检查文件大小，0KB 视为失败并删除 |
| URL 修正 | `normalizeUrl()` 强制 dn.qpyou.cn 使用 HTTP |
| 缓冲区 | 8192 bytes |

**关键代码**：

```kotlin
object DownloadManager {
    internal val _progress = MutableStateFlow(DownloadProgress())
    val progress: StateFlow<DownloadProgress> = _progress

    private fun normalizeUrl(url: String): String {
        return if (url.contains("dn.qpyou.cn")) {
            url.replace("https://", "http://")
        } else url
    }
}
```

### 4.3 DownloadService（前台下载服务）

**职责**：前台服务保障后台下载，通知栏显示进度，下载完成后触发安装

| 特性 | 说明 |
|------|------|
| 前台服务 | `foregroundServiceType="dataSync"`，确保下载不被回收 |
| 防重复启动 | `@Volatile isDownloading` 标志 |
| 进度通知 | 通过 `DownloadNotificationHelper` 更新通知栏 |
| Root 自动安装 | VERIFIED 后检查设置，执行 `su -c "pm install -r -g"` |
| 生命周期 | Root 安装完成后再 `stopSelf()`，避免取消安装协程 |
| 缓存清理 | 下载前自动清除旧安装包 |

**防止重复下载**：

```kotlin
companion object {
    @Volatile
    var isDownloading = false
        private set

    fun start(context: Context, url: String, versionName: String) {
        if (isDownloading) return  // 跳过重复请求
        // ...
    }
}
```

**Root 安装流程**（关键：安装完成后再 stopSelf）：

```kotlin
DownloadState.VERIFIED -> {
    // 检查 Root 自动安装设置
    if (rootAutoInstall && RootInstallHelper.isDeviceRooted()) {
        // 启动 Root 安装协程
        serviceScope.launch(Dispatchers.IO) {
            val result = RootInstallHelper.installSilently(progress.filePath)
            // 显示安装结果通知
            // 安装完成后才 stopSelf()
            stopSelf()
        }
    } else {
        stopSelf()  // 非 Root 场景直接停止
    }
}
```

### 4.4 DownloadNotificationHelper（通知助手，全局单例）

**职责**：管理下载进度通知栏显示，供 ViewModel 和 DownloadService 共用

| 状态 | 通知内容 | 图标 |
|------|----------|------|
| DOWNLOADING | 进度条 + 大小 + 速度 | `stat_sys_download` |
| VERIFYING | 不确定进度条 "校验中" | `stat_sys_download` |
| INSTALLING | 不确定进度条 "正在安装" | `stat_sys_download_done` |
| VERIFIED | "下载完成，点击安装" | `stat_sys_download_done` |
| FAILED / VERIFY_FAILED | "下载失败，请重试" | `stat_notify_error` |
| Root 安装成功 | "更新已自动安装完成" | `stat_sys_download_done` |
| Root 安装失败 | "自动安装失败: xxx" | `stat_notify_error` |

**通知 ID**：`1001`，**渠道 ID**：`download_channel`

### 4.5 RootInstallHelper（Root 静默安装工具）

**职责**：已 Root 设备的自动静默安装

| 方法 | 说明 |
|------|------|
| `isDeviceRooted()` | 检测 `which su` 及常见 su 路径 |
| `installSilently(apkPath)` | 执行 `su -c "pm install -r -g"` 覆盖安装并自动授予权限 |

**安装结果判定**：输出包含 "Success" 或 exitCode == 0 视为成功

**默认行为**：设置中检测到 Root 时默认开启，可手动关闭

**ViewModel 回退逻辑**：

```kotlin
fun installApk(): Boolean {
    if (rootAutoInstall && RootInstallHelper.isDeviceRooted()) {
        viewModelScope.launch {
            val result = RootInstallHelper.installSilently(filePath)
            if (result.success) {
                refreshInstalledInfo()
            } else {
                installViaSystemInstaller(context, file)  // Root 失败回退系统安装器
            }
        }
        return true
    }
    return installViaSystemInstaller(context, file)
}
```

### 4.6 VersionCheckWorker（定期检查 + 自动下载）

**职责**：WorkManager 定时检查版本更新，支持 WiFi 自动下载

```
WorkManager 定时触发
    │
    ▼
检查最新版本
    │
    ├── 有更新 → 读取用户设置
    │       │
    │       ├── [自动下载开启]
    │       │       ├── [仅WiFi + 当前WiFi] → DownloadService.start()
    │       │       ├── [不限网络] → DownloadService.start()
    │       │       └── [仅WiFi + 非WiFi] → 发送更新提醒通知
    │       │
    │       └── [自动下载关闭] → 发送更新提醒通知
    │
    └── 无更新 → 静默结束
```

**WiFi 检测**：`DownloadService.isWifiConnected(context)` 通过 `ConnectivityManager` 判断

### 4.7 WallpaperManager

**职责**：壁纸的下载、缓存、随机选择、透明度控制

| 壁纸源 | ID | 图片数量 | CDN 域名 |
|--------|-----|---------|----------|
| 魔灵召唤官网 | `official` | 19 张 | `event-fn.qpyou.cn` |
| SWC 电竞艺术 | `swc_art` | 37 张 | `hive-fn.qpyou.cn` |

**缓存路径**：`公共 Pictures/SWUpdater/wallpapers/`
**下载路径**：`公共 Download/SWUpdater/wallpapers/`

### 4.8 KeepAliveService

**职责**：后台保活，确保自动更新检查不被系统中断

| 保活方式 | 实现 |
|---------|------|
| 前台服务通知 | `START_STICKY` + 低优先级通知 |
| WakeLock | `PARTIAL_WAKE_LOCK`，6小时自动续期 |
| 电池优化白名单 | `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` |
| 开机自启动 | `BootReceiver` (BOOT_COMPLETED + QUICKBOOT + USER_PRESENT) |
| Root 保活 | `su -c` 降低 OOM 优先级 + renice |
| 心跳机制 | 15 分钟定时检查 + 续期 WakeLock |
| 服务重启 | `onDestroy()` 中自动重启 |

### 4.9 MainViewModel

**职责**：主界面数据管理，协调 UI 与业务逻辑

| StateFlow | 类型 | 说明 |
|-----------|------|------|
| `checkResult` | `UpdateCheckResult` | 版本检查结果 |
| `installedInfo` | `AppInstallInfo?` | 已安装应用信息 |
| `latestVersion` | `VersionInfo?` | 最新版本信息 |
| `downloadProgress` | `DownloadProgress` | 下载进度（共享 DownloadManager 单例） |
| `isChecking` | `Boolean` | 是否正在检查 |
| `sourceCheckResults` | `List<SourceCheckResult>` | 日志模式检测结果 |
| `logText` | `String` | 日志文本 |

### 4.10 AppInfoUtil

**职责**：检测本地魔灵召唤安装信息

**多包名自动检测**：

| 包名 | 说明 |
|------|------|
| `com.com2us.smon.normal.freefull.google.kr.android.common` | 全球服/Hive版（默认） |
| `com.com2us.smon.normal.freefull.google.kr.android.official` | 可能的官方版 |
| `com.com2us.smon.normal.freefull.google.kr.android` | 可能的短包名 |

**版本比较**：`parseVersionName("9.1.9") → [9, 1, 9]`，逐段比较

### 4.11 AppLog

**职责**：应用内日志系统

- 内存缓冲区：最多 500 条，FIFO
- 输出：Android Logcat + 内存缓冲 + 文件持久化
- 日志模式：设置中开关，开启后执行详细检测
- 实时监听：`addListener` 供 UI 展示

---

## 五、界面设计

### 5.1 主界面 (MainActivity)

```
┌──────────────────────────────┐
│  Toolbar: 魔灵召唤·自动更新  ⚙ │
├──────────────────────────────┤
│                              │
│  ┌─ 游戏信息卡片 ──────────┐ │
│  │ 🎮 魔灵召唤：天空之役    │ │
│  │ com.com2us.smon...       │ │
│  │                          │ │
│  │ ┌─当前版本─┐┌─最新版本─┐ │ │
│  │ │  9.1.7   ││  9.1.9   │ │ │
│  │ └──────────┘└──────────┘ │ │
│  │ 🟡 发现新版本            │ │
│  └──────────────────────────┘ │
│                              │
│  [检查更新]  [立即下载]       │
│  [安装更新]  [启动游戏]       │
│                              │
│  ┌─ 下载进度卡片 ──────────┐ │
│  │ ████████░░ 78%           │ │
│  │ 94.1 MB / 120.5 MB      │ │
│  │ 🔄 校验中...             │ │
│  └──────────────────────────┘ │
│                              │
│  [魔灵工具箱]  [反馈留言]    │
│  Design by 啊啊啊黑猫警长魔灵│
│                              │
│                    [📁][⬇️][🔄]│
│                   打开 下载 随机│
│                   目录 壁纸 壁纸│
└──────────────────────────────┘
```

### 5.2 设置界面 (SettingsActivity)

| 分类 | 设置项 | 类型 | 默认值 |
|------|--------|------|--------|
| **外观** | 主题模式 | DropDown | 跟随系统 |
| **壁纸** | 启动时自动更换壁纸 | Switch | true |
| | 内容透明度 | SeekBar (20-100) | 70 |
| | 壁纸源 | DropDown | 魔灵召唤官网 |
| | 缓存壁纸数量 | DropDown | 10 |
| | 清除壁纸缓存 | Button | - |
| | 壁纸下载目录 | EditText | 默认 |
| **更新设置** | 自动检查更新 | Switch | true |
| | 检查频率 | DropDown | 每6小时 |
| | 自动下载 | Switch | false |
| | 仅 Wi-Fi 下载 | Switch | true |
| **后台保活** | 启用后台保活 | Switch | false |
| | 开机自启动 | Switch | true |
| | 忽略电池优化 | Button | - |
| | Root 保活 | Switch | false |
| | **Root 自动安装** | **Switch** | **isDeviceRooted()** |
| | 保活说明 | Text | - |
| **安全设置** | 校验完整性 | Switch | true |
| **调试** | 日志模式 | Switch | false |
| | 查看日志 | Button | - |
| | 导出日志 | Button | - |
| **数据源** | 更新源地址 | EditText | qpyou.cn |
| | 游戏包名 | EditText | com.com2us... |
| **其他** | 清除下载缓存 | Button | - |
| | 应用版本 | Text | v1.7.0 |

---

## 六、数据模型

### 6.1 VersionInfo

```kotlin
data class VersionInfo(
    val versionName: String,       // "9.1.9"
    val versionCode: Long = 0,
    val downloadUrl: String = "",  // APK 直链
    val fileSize: Long = 0,        // 字节
    val md5: String = "",
    val sha256: String = "",
    val changelog: String = "",
    val releaseDate: String = "",
    val downloadChannels: List<DownloadChannel> = emptyList()
)
```

### 6.2 DownloadState

```kotlin
enum class DownloadState {
    IDLE,           // 空闲
    DOWNLOADING,    // 下载中
    PAUSED,         // 已暂停
    DOWNLOADED,     // 已下载
    VERIFYING,      // 校验中
    VERIFIED,       // 校验通过
    VERIFY_FAILED,  // 校验失败
    INSTALLING,     // 安装中
    FAILED          // 失败
}
```

### 6.3 DownloadProgress

```kotlin
data class DownloadProgress(
    val state: DownloadState = DownloadState.IDLE,
    val downloadedBytes: Long = 0,
    val totalBytes: Long = 0,
    val speed: Long = 0,           // 字节/秒
    val filePath: String = ""
) {
    val progressPercent: Int       // 0-100
    val isCompleted: Boolean       // DOWNLOADED 或 VERIFIED
}
```

### 6.4 UpdateCheckResult (Sealed Class)

```kotlin
sealed class UpdateCheckResult {
    data class UpToDate(val currentVersion: String)
    data class UpdateAvailable(val currentVersion: String, val latestVersion: VersionInfo)
    data class Error(val message: String)
    data object Checking
}
```

### 6.5 InstallResult

```kotlin
data class InstallResult(
    val success: Boolean,
    val message: String
)
```

### 6.6 DownloadChannel

```kotlin
data class DownloadChannel(
    val id: String,               // "qpyou"
    val name: String,             // "友皆乐（官方推荐）"
    val url: String,              // 下载链接
    val type: ChannelType,        // APK_DIRECT / OFFICIAL_WEB / APP_STORE / ACCELERATOR / CUSTOM
    val description: String = "",
    val isRecommended: Boolean = false,
    val requireVpn: Boolean = false
)
```

### 6.7 WallpaperDownloadResult

```kotlin
data class WallpaperDownloadResult(
    val success: Boolean,
    val filePath: String = "",
    val fileName: String = "",
    val error: String = ""
)
```

---

## 七、权限清单

| 权限 | 用途 |
|------|------|
| `INTERNET` | 网络访问 |
| `ACCESS_NETWORK_STATE` | 网络状态检测（WiFi判断） |
| `WRITE_EXTERNAL_STORAGE` | 下载文件 (Android 9 及以下) |
| `READ_EXTERNAL_STORAGE` | 读取文件 (Android 12 及以下) |
| `MANAGE_EXTERNAL_STORAGE` | 管理外部存储 (Android 11+) |
| `REQUEST_INSTALL_PACKAGES` | 安装 APK |
| `FOREGROUND_SERVICE` | 前台服务 |
| `FOREGROUND_SERVICE_DATA_SYNC` | 前台服务类型 |
| `POST_NOTIFICATIONS` | 通知权限 (Android 13+) |
| `RECEIVE_BOOT_COMPLETED` | 开机自启 |
| `WAKE_LOCK` | 防止 CPU 休眠 |
| `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | 电池优化白名单 |
| `SCHEDULE_EXACT_ALARM` | 精确闹钟 |
| `USE_EXACT_ALARM` | 精确闹钟 (Android 13+) |

---

## 八、依赖库

| 库 | 版本 | 用途 |
|----|------|------|
| AndroidX Core KTX | 1.12.0 | Kotlin 扩展 |
| AppCompat | 1.6.1 | 兼容性支持 |
| Material Components | 1.11.0 | Material Design 3 |
| ConstraintLayout | 2.1.4 | 布局 |
| SwipeRefreshLayout | 1.1.0 | 下拉刷新 |
| Preference KTX | 1.2.1 | 设置界面 |
| Lifecycle | 2.7.0 | ViewModel + LiveData |
| Coroutines | 1.7.3 | 异步编程 |
| OkHttp | 4.12.0 | HTTP 客户端 + IPv4 优先 DNS |
| Jsoup | 1.17.2 | HTML 解析 |
| Gson | 2.10.1 | JSON 序列化 |
| DataStore | 1.0.0 | 数据存储 |
| WorkManager | 2.9.0 | 后台任务调度 |

---

## 九、关键算法

### 9.1 版本号提取

```
输入: http://dn.qpyou.cn/smon/smon_919_NucRxWzPu7G4C2.apk

1. 提取文件名: smon_919_NucRxWzPu7G4C2.apk
2. 正则匹配: smon_(\d{3,}) → 919
3. 数字转版本:
   - 3位 (919): X.Y.Z → 9.1.9
   - 4位 (9120): X.Y.ZZ → 9.1.20
   - 5位+ (91200): X.Y.ZZZ → 9.1.200
```

### 9.2 URL 协议修正

```
多层防御确保 dn.qpyou.cn 使用 HTTP:

1. VersionCheckService.ensureHttps()
   解析 APK 链接时强制 HTTP

2. VersionCheckService.fetchApkFileSize()
   HEAD 请求获取文件大小时强制 HTTP

3. DownloadManager.normalizeUrl()
   下载时再次强制 HTTP（安全网）
```

### 9.3 透明度计算

```
alphaPercent (0-100, 设置值)
    │
    ▼
alpha255 = alphaPercent * 255 / 100

遮罩层: Color.argb(alpha255, baseR, baseG, baseB)
  日间 base = #FFFFFF
  夜间 base = #0D0B1A

卡片:   Color.argb(cardAlpha255, cardR, cardG, cardB)
  日间 card = #FFFFFF
  夜间 card = #1E1B35

版本小卡: Color.argb(cardAlpha255, versionR, versionG, versionB)
  日间 version = #F3EFFF
  夜间 version = #252244
```

### 9.4 下载速度计算

```
每秒采样一次:
speed = (当前已下载字节 - 上次采样字节) * 1000 / 时间差(ms)
```

### 9.5 Root 静默安装

```bash
# 执行命令
su -c "pm install -r -g \"/path/to/app.apk\""

# 参数说明
# -r: 覆盖安装（保留数据）
# -g: 自动授予所有权限

# 成功判定
# 输出包含 "Success" 或 exitCode == 0
```

---

## 十、存储路径

| 内容 | 路径 | 说明 |
|------|------|------|
| APK 下载 | `公共 Download/SWUpdater/updates/` | 卸载不丢失 |
| 壁纸缓存 | `公共 Pictures/SWUpdater/wallpapers/` | 卸载不丢失 |
| 壁纸下载 | `公共 Download/SWUpdater/wallpapers/` | 卸载不丢失 |

> 使用 `Environment.getExternalStoragePublicDirectory()` 而非 `context.getExternalFilesDir()`，
> 确保应用卸载后文件不丢失，且在文件管理器中可见。
> Android 10 兼容通过 `requestLegacyExternalStorage="true"` 实现。

---

## 十一、构建与运行

### 11.1 环境要求

- Android Studio Hedgehog | 2023.1.1+
- JDK 17
- Android SDK 34
- Kotlin 1.9.x

### 11.2 构建命令

```bash
# Debug 版本
./gradlew assembleDebug

# Release 版本
./gradlew assembleRelease

# 输出路径
# app/build/outputs/apk/debug/app-debug.apk
# app/build/outputs/apk/release/app-release.apk
```

### 11.3 签名配置

Release 签名需在 `app/build.gradle.kts` 中配置：

```kotlin
android {
    signingConfigs {
        create("release") {
            storeFile = file("keystore.jks")
            storePassword = "..."
            keyAlias = "..."
            keyPassword = "..."
        }
    }
}
```

---

## 十二、版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0.0 | - | 初始版本：版本检测、APK下载、安装 |
| v1.1.0 | - | 修复版本检测，增加日志模式、日/夜模式 |
| v1.2.0 | - | 修复 Google Play IPv6、Com2uS DNS、本地版本检测 |
| v1.3.0 | - | 国内下载方案，qpyou.cn 作为主数据源 |
| v1.4.0 | - | 应用图标、底部Footer、壁纸系统 |
| v1.5.0 | - | 内容透明化、FAB按钮、数据源URL统一 |
| v1.6.0 | - | 壁纸透明度设置、FAB移至右下角、刷新图标 |
| **v1.7.0** | 2025-04 | 缓存目录迁移至公用目录、修复 64KB/0KB 下载问题、OkHttp 替代 HttpURLConnection、去除渠道选择直接下载、下载前自动清除旧缓存、WiFi 自动下载 + 通知栏进度、DownloadManager 全局单例、Root 自动静默安装、后台保活 |

---

## 十三、已知问题与待优化

| 问题 | 优先级 | 说明 |
|------|--------|------|
| 下载渠道单一 | 中 | 目前只有友皆乐一个渠道 |
| Root 保活/安装 su 命令可能失败 | 中 | 部分设备 su 权限受限或 Magisk 隐藏 |
| 无推送通知 | 低 | 可集成 Firebase / 极光推送 |
| Android 11+ Scoped Storage | 低 | 当前用 requestLegacyExternalStorage 兼容，未来需迁移至 SAF |
